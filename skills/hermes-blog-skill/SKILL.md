---
name: hermes-blog-skill
description: Track, research, verify, and publish one genuinely new GroomLocal grooming article with newly generated Maui artwork per weekly run. Do not refresh existing guides. Uses live SEO evidence and the approved Maui design standard, with publication controlled by the job's explicit mode.
---

# Hermes Blog Skill

This workflow is for GroomLocal, not TAILOR or any other business in shared Hermes identity files. Work only on the grooming directory task. The project root is the current GroomLocal checkout (the directory containing `package.json` and `src/content/blog/`). Read [setup on another computer](references/setup.md) before the first run. Repository paths below are relative to that root; bundled `references/` paths are relative to this skill folder. Do not send emails, contact groomers, or message external channels. The scheduler saves your final report locally.

## Authority and output

The user authorized recurring GTM tracking and verified publication on September 5, 2026. Run `python3 scripts/weekly-guides-preflight.py` from the project root at the start unless a valid preflight result was already supplied. It supplies the run directory, inventory, and execution mode. The scheduler does not inject a script because its script-directory restriction rejects this repository path. In **publish** mode, choose one worthwhile NEW guide with a NEW Maui illustration, track it on the GroomLocal GTM board, prepare and verify the article and artwork, then publish through `scripts/publish-weekly-guide.py`. In **review** mode, stop after the verified draft. Read the current policy; do not alter your own permissions, schedules, model, credentials, or unrelated code. Never deploy the dirty working checkout or commit/push it wholesale.

## GTM tracking on every run

Use the `gtm-board` MCP tools with the explicit project **groomlocal**. Never guess grooming/groomhub or fall back to TAILOR. At the beginning inspect existing SEO cards and create/update one card in `preparing` for this run, type `seo`, channel `seo`, target date in Pacific time, tags `weekly-guides` and the chosen slug when known. Record the returned card ID in `gtm-task.json`. Update its title when the topic is selected and add research, draft, report, and verification paths. A board-status update must preserve existing draft, sources, evidence, and report files; it updates only the board and gtm-task.json. Deduplicate by stored card ID or matching run marker before creating anything.

## Agent board tracking (recurring task)

The weekly job also has one permanent card on the GroomLocal **agent-tasks** board, task_id `weekly-groomlocal-grooming-guide-mondays-9am-pt-7725` (card UUID `684565f9-bf2b-4591-bd20-b91d43ae5edf`). This is where Camren watches the job. Update it on every run, in addition to the per-article SEO card on the marketing board:

- At the start of the run call `gtm_update_agent_task` with project `groomlocal`, that task_id, `column: "preparing"` (do not pass `status`, which would move the column), and an `output` object. Read the current output with `gtm_get_agent_task` first and preserve `output.runs`; append a new entry `{date, slug, title, url, deployment_id, primary_query, marketing_card, result: "in_progress"}` and set `output.last_run` to today's Pacific date.
- After publication succeeds (`publication.json` says `live` and the URL was checked), call `gtm_update_agent_task` again with `column: "preparing"` and the same output with that run's `result` set to `"published"`, the canonical URL, deployment ID, and the marketing card slug. Include `content_type: "new"` and `new_maui_image: true` only after verifying those outcomes. Also append a row to the "Run log" table in the card body via `title`/`details` if the tool supports it; otherwise the `output.runs` log is sufficient.
- If the run publishes nothing, set `result` to `"no_change"` or `"blocked"` with a one-line `reason`, and `output.last_result` accordingly.
- Never pass `status: "completed"` for this task and never move it to `done`. It is an ongoing weekly job and must stay in `preparing`, which the dashboard shows as IN PROGRESS (`live` shows as BLOCKED there). Only the per-article marketing card moves columns.
- If the agent-task card is missing, recreate it with `gtm_create_agent_task` (project `groomlocal`, column `preparing`, assigned_agent `hermes`, tags `weekly-guides`, `recurring`, `seo`), record the new task_id in `gtm-task.json` under `agent_task_id`, and report that it was recreated.

After every agent-task update, read the task back and verify all of the following: the recurring card is still in `preparing`, the current run appears exactly once in `output.runs`, `output.last_run` and `output.last_result` are current, and the human-readable Run log body contains the current row when the update tool accepts `body`/`details`. Do not assume a successful tool response means the body and structured output were both persisted.

Before publication, the card must exist. If the board is unavailable, retain complete draft/task details locally and report the blocker; do not claim a board write. Retry once for a concrete transient error, then stop publication until tracking is restored. A failed release stays in `preparing` with the specific blocked check. Only after `publication.json` says `live` and the URL has been checked, move the card to `live` and include the canonical URL, deployment ID, primary query, evidence date, and verification outcome. If this final board update fails, preserve `publication.json` and a pending update in `gtm-task.json`; the next run must synchronize pending updates before selecting new work. Never republish a successful run just because its board update failed.

The user clarified on September 7, 2026 that Codex maintains the existing guides and Hermes must create NEW articles with NEW Maui illustrations. Prepare at most one genuinely new, nonoverlapping article per run. Never refresh, retitle, or duplicate an existing guide to satisfy this job. If the evidence does not justify a new article, save a short no-change report with the next opportunity to investigate. Do not change dates without meaningful updates, rewrite winning articles merely to make them longer, or fill a weekly quota with duplicate content.

## Research and selection

Read [SEO and editorial guidance](references/seo-editorial.md) for the editorial and Maui requirements. This scheduled workflow already authorizes research, drafting, GTM card updates, and publication through the verified command when policy mode is publish, so do not stop for brainstorming approval.

### Preflight path-policy fallback

If the scheduler rejects the repository preflight because its path resolves outside the configured Hermes scripts directory, do not treat the error as a successful preflight and do not publish from incomplete metadata. Record the exact error in the run report, then run the same preflight directly from the project root (`python3 scripts/weekly-guides-preflight.py`) if repository execution is permitted. Continue only after the direct run returns valid inventory, run directory, and mode; otherwise save a blocked report. Keep this workaround documented as a policy limitation rather than modifying schedules, permissions, or unrelated tooling. Preserve the useful research sequence using available tools; unavailable Superpowers skills do not block drafting.

Read the preflight inventory, the shared `docs/automation/content-inventory.json` coverage snapshot, `src/lib/grooming-guides.ts`, and the latest report under `docs/content-audits/`. The coverage snapshot includes local drafts and guides that may be absent from the Git checkout; reconcile it with live pages and GTM, and do not assume every entry is published. Check existing coverage before proposing a new slug. Prioritize demonstrated search opportunity, real reader questions, regional relevance, and gaps not already answered by existing guides. Existing guides are coverage exclusions and internal-link targets, not candidates for revision. Record the closest existing guides and explain how the selected intent differs. Seasonality can inform selection; keyword volume alone does not establish intent or value.

Use the DataForSEO tools for:

1. Existing domain rankings, preserving each result's collection date. Prefer Search Console clicks, impressions, and queries. GroomLocal GSC was verified through GTM Board's `google_search_console` connector for `sc-domain:groomlocal.com` on September 5, 2026; check that route before declaring GSC unavailable just because no standalone GSC tool exists. Reverify access and use the requested date range, label preliminary dates, and distinguish search clicks from inquiries or bookings. Third-party rankings and estimated traffic are not analytics.
2. One batch of roughly 10–15 candidate keywords, then live SERPs for the top 3 relevant candidates. Record organic results, relevant People Also Ask questions, related searches, and any available AI Overview sources.
3. A bounded check of Google and ChatGPT citations for the chosen topic and GroomLocal, plus backlink ranks for GroomLocal and a few actual competitors. For domain LLM filters use `search_scope: ["any"]`; this API rejects `answer` for domains. Distinguish missing data, empty results, and tool errors.

Record source URLs, retrieval dates, and compact raw metrics under the run directory. Preserve the exact requested keyword list and raw returned records; derive missing-keyword lists from those records so returned keywords cannot also be labeled missing. For large persisted tool results, save the relevant raw file and inspect compact ranking/keyword fields instead of loading unrelated output into context. Do not invent volumes, click counts, rankings, quotes, credentials, discounts, or market averages. Advertising competition is not organic keyword difficulty. Treat backlink scores as comparative context; no fixed score gap guarantees a ranking outcome or makes authority irrelevant to AI citations.

If an optional citation or authority endpoint fails, record the limitation and proceed with the evidence you have. If core search/research tools are unavailable, produce a blocked report instead of an unsupported article. Avoid unlimited retries: one corrected retry for a concrete input or transient error, then report the failure.

## Draft the guide

Use the current MDX schema and existing author attribution; do not invent professional relationships or qualifications. Do not edit existing guides. Set the new article publication date to the actual Pacific publication day and use the same initial dateModified. Choose a nonoverlapping slug and the right `GUIDE_TOPICS` group; include a small proposed topic-mapping patch with the draft.

Lead with a direct answer, use meaningful headings, and answer the reader's actual decision. Link prices and changing claims directly to current primary sources in the article body, not only a separate report. A provider blog describing general market prices is not its current menu; label old estimates accurately or exclude them. Do not promote a SERP snippet into verified provider pricing when the page cannot be checked. Label a small set of examples as a sample, never as a citywide or national average. Distinguish bath/tidy/full haircut, provider weight brackets, starting prices, and hourly charges. Check geography carefully: Portland, Oregon is not Portland, Maine. Use veterinary or other authoritative sources for animal-health claims and avoid diagnosis or individualized treatment advice.

Use natural internal links to relevant existing guides, service/city pages, and `/get-quotes` where helpful. Call the section **Grooming Guides**, at `/blog`; `/resources` redirects there. Do not imply that every listing offers confirmed prices, availability, credentials, or instant booking.

## Maui artwork

Every new article requires a newly generated, topic-specific Maui illustration. Reusing or renaming a production image does not satisfy this requirement. Before generating, editing, or choosing new artwork, read the bundled [Maui style standard](references/maui/STYLE-STANDARD.md), [base prompt](references/maui/MAUI-BASE-PROMPT.md), and [image workflow](references/maui-image.md). Inspect [the approved brushing reference](references/maui/approved-reference/brushing-v2.png). The setup reference explains their canonical repository locations. These immutable September 2026 references govern appearance, not historical mascots or later generations.

Preserve Maui's face, compact four-limbed dog anatomy, burgundy outlines, cream/tan cel shading, and red MAUI bandana. Follow the canonical generation and transparent-export workflow. Save candidates and masters separately from derivatives in the run directory. Generate only in the run directory or an isolated staging copy. Generate a new scene for this article with the fixed approved appearance reference. Save the generation prompt, reference path, model/tool, opaque master if applicable, and verified transparent PNG in the run directory. Do not replace this step with an existing asset. Use the installed image helpers from an isolated staging copy when no image-generation MCP tool is exposed; inspect helper arguments before execution. Do not load a different business identity or modify credentials. For temporary setup, create a unique directory inside the run directory with tempfile.mkdtemp and retain that exact returned path. Do not delete a shared scratch path or rediscover it with find /tmp (macOS uses a symlink there). Copy only the helper, fixed reference, and prompt files that are needed; avoid copying the whole checkout. Pass existing image credentials only to the child process environment, without printing or saving their values into run artifacts. Honor image helpers' executable shebangs (including uv dependency setup), or invoke uv run --script explicitly. Do not replace that runtime with bare python3; verify required image-library imports before making a paid generation request.

Before treating an image as ready, inspect it at full size and display size on cream, teal, and dark backgrounds. Check actual alpha, intact fur interiors, readable bandana, complete props, unclipped edges, and the standard's dimensions/margins. Inspect the extractor's numbered enclosed-region diagnostic at full size and explicitly record which candidate IDs are background gaps to remove and which must stay as fur, clothing or props. Check spaces inside table supports and between arms, bodies and legs individually; transparent corners and a generic vision-tool pass do not prove those enclosed gaps are transparent. Re-extract from the preserved master with reviewed seeds when needed; this is export correction, not a new image-generation round. If no working image or vision tool is available, mark the new artwork pending and report the specific unavailable tool; never claim an unperformed visual check. Cap new scene work at one initial generation round and one correction round for a concrete defect.

## Verification and publication

Save `drafts/<slug>.mdx`, `sources.md`, `seo-evidence.json`, the proposed topic, and `report.md` in the run directory. Verify frontmatter, MDX compilation, factual support, direct citations, real internal routes, title/excerpt accuracy, author attribution, and Maui appearance/alpha checks. Do not change a new article to a broad claimed average based on a few examples. Review the complete draft after writing; correct unsupported claims before signing off.

Before publishing, create `release-checks.json` with actual booleans `sources_verified`, `editorial_reviewed`, `image_verified`, `internal_links_verified`, plus `draft_sha256` of the exact final draft and notes naming the sources and visual checks performed. Never mark an unperformed check true. The GTM card ID must be stored as a string in `gtm-task.json` under `card_id` or `task.id`.

Run from the project root:

```bash
python3 scripts/publish-weekly-guide.py --run "<run directory>" --slug "<slug>" --topic "<GUIDE_TOPICS id>" --new --asset "<run directory>/<verified PNG>"
```

Always pass both `--new` and `--asset "<run directory>/<verified PNG>"`, and use `/maui-assets/<slug>.png` in the MDX. The publisher rejects existing-article revisions and missing or reused production artwork under the new-only policy. New assets already have unique filenames, so use the prescribed query-free image path. If any image URL contains a cache-version query, verify that its complete path and exact query are allowed by next.config.ts images.localPatterns. A raw PNG returning HTTP 200 does not prove the page image loads: the release gate must request the actual optimized src/srcset URLs emitted in article and listing HTML, at mobile/desktop sizes and the src fallback, and require valid image responses. Never mark an optimizer HTTP 400/404 as a successful image check. The command reconstructs the current production source by hash, allows only the chosen MDX, topic assignment, and optional PNG, compiles MDX, checks image alpha and links, builds a production-target deployment without promoting it, checks the article/list/feed/sitemap, and promotes only if production still matches its starting baseline. It then verifies the public URL and records `publication.json`.

If a check fails, inspect its concrete error and correct it; do not bypass a check or publish manually. If production changes during the build, stop and report the conflict. Preserve the staged files and manifests for review. Do not blindly retry a deploy after an ambiguous promotion result: inspect the live deployment first. The command refuses to overwrite an existing stage and recognizes previously completed publication records. After fixing a concrete verification error, `--resume` reverifies the existing deployment without building again: production must still match the baseline, and every deployed source hash must match the reviewed stage. Do not use it to skip a failed check.

If deployment promotion appears to have succeeded but the publisher exits during its final HTTP route check and does not write `publication.json`, follow `references/post-promotion-verification.md`. Inspect the deployment result and identify the failing route before any retry; independently serving the article is not the same as completing the controlled publication contract. Do not fabricate or hand-author a live publication record unless the repository explicitly documents that recovery path, and do not move GTM cards to live while the required publication record is absent.

After successful publication, update the GTM card as described above and return the live URL, card ID, chosen query and rationale, and checks. In review mode return the draft paths with `Not published — ready for review`. If no article is justified, record why on the task and report no change. Keep credentials out of all artifacts. For historical preflight and GTM synchronization notes, see `references/2026-09-07-run-notes.md`.
