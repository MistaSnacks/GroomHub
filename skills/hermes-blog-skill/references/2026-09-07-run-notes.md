> Historical record from the retired code-deployment workflow. For new runs use [Snackbox publication](snackbox-publishing.md); do not execute the old publisher steps below.

# 2026-09-07 run notes

Historical incident only. The refresh described below predates the new-only clarification later that day. Current runs must create new articles and new artwork; this incident does not authorize refreshes.

## Preflight path-policy error

The scheduler's data-collection invocation rejected the repository script because it resolved outside `<HERMES_HOME>/scripts`:

```text
Blocked: script path resolves outside the scripts directory (<HERMES_HOME>/scripts): '<GROOMLOCAL_ROOT>/scripts/weekly-guides-preflight.py'
```

Running the same script from `<GROOMLOCAL_ROOT>` with `python3 scripts/weekly-guides-preflight.py` returned valid metadata and exit code 0. The error remained documented in the report; no scheduler, permission, credential, or unrelated-code changes were made.

## Recurring GTM task synchronization

The structured `output.runs` update succeeded before the human-readable task body was checked. A follow-up read exposed that the Run log table was stale. The body was then updated explicitly with the new row and read back for verification. Future runs should send and verify both structured output and body content, preserving all prior runs and keeping the recurring task in `preparing`.

## Publication outcome

Existing slug `cat-grooming-what-to-expect` was substantively refreshed and published. The marketing card moved to `live` only after `publication.json` reported `live` and the public URL returned HTTP 200. The recurring agent task remained in `preparing` with `last_result: published`.
